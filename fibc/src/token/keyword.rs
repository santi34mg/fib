#[derive(Debug, Clone, PartialEq)]
pub enum Keyword {
    Const,
    If,
    Else,
    For,
    Break,
    Continue,
    Return,
    Function,
    Switch,
    When,
    Struct,
    Enum,
    Union,
}
