#[derive(Debug, Clone, PartialEq)]
pub enum Keyword {
    Const,
    Var,
    If,
    Else,
    For,
    While,
    Break,
    Continue,
    Return,
    Function,
    Switch,
    When,
    Struct,
    Enum,
    Union,
    Type,
    As,
    Extern,
    Defer,
    Import,
}
