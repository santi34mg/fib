#[derive(Debug, Clone, PartialEq)]
pub enum Keyword {
    Const,
    Var,
    If,
    Else,
    For,
    Break,
    Continue,
    Return,
    Function,
    Switch,
    When,
    Struct,
    Enum,
    Union,
}
