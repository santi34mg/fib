// Plain enum discriminant cast.
// Expected output:
//   color discriminant = 1
import std::libc

type Color enum {
    Red,
    Green,
    Blue,
}

fn main() @int4 {
    c: Color = Color.Green;
    libc::printf("color discriminant = %d\n", c as @uint8);
    return 0;
}
