import std::libc

type Inner struct {
    uint8 value,
}

type Outer struct {
    Inner inner,
}

fn main() int4 {
    o: Outer = Outer { inner: Inner { value: 0 } }
    o.inner.value = 42
    libc::printf("got %d\n", o.inner.value)
    ret 0
}
