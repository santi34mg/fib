#[derive(Debug, Clone, PartialEq)]
pub enum Keyword {
    Var,
    If,
    Else,
    For,
    Break,
    Continue,
    Return,
    Function,
    Switch,
    When,
    Struct,
    Enum,
    Union,
    Type,
    As,
    Extern,
    Defer,
    Import,
}
